
module.exports = {

    base_url: "https://example.com",

    collectives: {
        sudoroom: {
            name: "sudo room",
            privs: [], // privileges supported by this collective
            memberships: {
                base: 10 // base membership is $10 a month or more
            },

            stripe_api_key: undefined, // Add your Stripe API key
            stripe_publishable_key: undefined // Add your Stripe publishable key
        },
        ccl: {
            name: "Counter Culture Labs",
            privs: ['admin'], // privileges supported by this collective
            memberships: {
                food_safe_only: 20,
                bsl_1: 80
            },

            stripe_api_key: undefined, // Add your Stripe API key
            stripe_publishable_key: undefined // Add your Stripe publishable key
        }
    },

    mailer: {
        type: "smtp", // can also be "direct" or "console"
        host: "localhost", // only needed for type: "smtp"
        port: 25, // only needed for type: "smtp"
        tls: false,
        from_address: "noreply@example.com"
    }
};
